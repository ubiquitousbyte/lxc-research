#include <conty/conty.h>

int main(int argc, char *argv[])
{
    /*
     * This will be the CLI application that configures the benchmark
     * It will consist of an event loop for managing the container processes
     * and a set of hooks that sample disk, network, and scheduling counters
     * from the kernel for all of them
     */
    return 0;
}